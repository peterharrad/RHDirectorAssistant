# tests/fixtures/

Short, hand-written snippets for the unit tests: a few headings, a definitions
table, a clause with sub-clauses. Whole documents live in `sources/` and are covered
by the golden test instead.
